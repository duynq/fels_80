<div class="users" >
  <section class="user_info">
    <div>
      <%= gravatar_for user, size: Settings.gravatar_size %>
      <h1 class="user_name"> <%= link_to user.name, user %></h1>
    </div>|
    <%= link_to t("users.destroy.delete"), admin_user_path(user), method: :delete,
      data: { confirm: t("users.destroy.confirm") } %>
  </section>
</div>
